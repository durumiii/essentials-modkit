# modkit — Essentials 팬게임 모드 표준과 매니저

## 배경과 목표

포켓몬 Z 한글패치 v5 배포 후 유저들이 이전 패치가 남은 설치본 위에 새 패치를 얹다 버그를 만나는 일이 반복되고 있다. fanlib의 모드 관리 코어(modstore, modassets, modfit, rubyread, rubywrite)는 이 문제를 풀 재료 — 모드 카드(mod.json), 세 가지 설치 계약, expects 해시 검증, 베이스라인 — 를 이미 갖추고 있으나 개인 라이브러리 속 부품이라 공개할 수 없다. modkit은 이 코어를 독립 프로젝트로 이사시켜 Essentials 팬게임 전반에 통하는 모드 단위·관리 규약을 스펙 문서로 확립하고, CLI와 GUI를 갖춘 스탠드얼론 도구로 배포한다. 제작자가 모드를 선언하고 유저가 안전하게 설치·관리할 수 있는 표준을 만드는 것이 목표다.

## 스펙과 계획

- **스펙**: 이 저장소의 `docs/design.md` 참고. 모드 정의, 카드 형식 v1, 설치 계약 3종, 안전 규약, 매니페스트와 진단, 호환성 검증을 포함한다.
- **구현 계획**: 상위 저장소 pokemon-fangame-kr의 `docs/superpowers/plans/2026-08-03-modkit.md` 참고.

## 안전 규약

- 첫 쓰기 전 `.orig` 백업을 생성하며, 이미 존재하면 덮지 않는다.
- 파일 쓰기 전에 직렬화→역직렬화 왕복 검증을 수행하고, 원자적 교체(임시 파일 작성 후 rename)로 하드링크 함정을 회피하며, CRLF 형식을 보존한다.
- 모드 카드의 `game` 필드와 설치본 제목이 다르면 WrongGame 예외로 거부하여 게임 귀속을 강제한다.
- 삭제 동사는 사용하지 않으며, 대신 외래 파일을 게임 폴더 안 `_quarantine/<날짜>/`로 격리하여 통째로 복구할 수 있도록 한다.

---

**작업 노트**: Task 1은 패키지 스캐폴드를 확립한다. 이후 태스크는 모드 카드 모듈, 설치 계약 구현, 진단 엔진, GUI 등을 순차적으로 구현한다.
